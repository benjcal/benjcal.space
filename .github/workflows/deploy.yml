name: Build & Deploy

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    container: alpine:3.24
    steps:
      - name: Install dependencies
        run: apk add --no-cache git rclone curl

      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Zola
        uses: taiki-e/install-action@v2
        with:
          tool: zola

      - name: Build site
        run: zola build

      - name: Sync to BunnyCDN storage
        env:
          RCLONE_CONFIG_BUNNY_TYPE: bunny
          RCLONE_CONFIG_BUNNY_STORAGE_ZONE: ${{ secrets.BUNNY_STORAGE_ZONE }}
          RCLONE_CONFIG_BUNNY_KEY: ${{ secrets.BUNNY_STORAGE_PASSWORD }}
        run: |
          set -euo pipefail
          test -n "$RCLONE_CONFIG_BUNNY_STORAGE_ZONE"
          test -n "$RCLONE_CONFIG_BUNNY_KEY"
          rclone help backends | grep -q bunny
          rclone sync public/ bunny: --verbose

      - name: Purge CDN cache
        env:
          BUNNY_API_KEY: ${{ secrets.BUNNY_API_KEY }}
          BUNNY_PULLZONE_ID: ${{ secrets.BUNNY_PULLZONE_ID }}
        run: |
          set -euo pipefail
          test -n "$BUNNY_API_KEY"
          test -n "$BUNNY_PULLZONE_ID"
          curl --request POST \
            --url "https://api.bunny.net/pullzone/${BUNNY_PULLZONE_ID}/purgeCache" \
            --header "AccessKey: ${BUNNY_API_KEY}" \
            --header "content-type: application/json"
